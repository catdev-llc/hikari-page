---
const cards = [
  { stat: '32', title: 'Monitored ports', desc: 'Simulated multi-route fiber topology with realistic event patterns and fault injection.' },
  { stat: 'Live', title: 'Continuous analysis', desc: 'Full pipeline running 24/7 — ingest, trace comparison, root cause scoring, and incident delivery.' },
  { stat: '~5 min', title: 'Fault to report', desc: 'From injected fault to structured incident package with confidence score and dispatch recommendation.' },
];
---

<section class="py-[5.5rem] border-b border-hikari-border-soft" id="demo">
  <div class="wrap">
    <span class="label rv">Live Environment</span>
    <h2 class="text-hikari-text-strong font-semibold tracking-tight leading-[1.2] text-[clamp(1.5rem,2.8vw,2.1rem)] mb-3.5 rv">
      See it running. 32-port simulation, live analysis.
    </h2>
    <p class="desc rv">
      Hikari is not a roadmap item. A fully operational environment with 32 simulated OTDR ports
      is running now — generating alarms, analyzing traces, and producing structured incident reports
      in real time. Request access to see the pipeline working end-to-end on realistic fault scenarios.
    </p>

    <div class="demo-grid grid grid-cols-3 gap-3 mt-5">
      {cards.map((card) => (
        <article class="border border-hikari-border rounded-lg bg-hikari-bg-2 p-4 rv">
          <div class="text-[2rem] font-bold text-hikari-accent tracking-tight font-mono mb-1">{card.stat}</div>
          <h3 class="text-hikari-text-strong font-semibold text-[1.05rem] leading-[1.2] mb-1.5">{card.title}</h3>
          <p class="text-hikari-muted text-[0.82rem] leading-relaxed">{card.desc}</p>
        </article>
      ))}
    </div>

    <div class="mt-4 border border-hikari-accent/25 rounded-lg bg-hikari-accent/[0.08] p-4 flex items-center justify-between gap-6 flex-wrap rv">
      <p class="text-[#b3ece0] text-[0.85rem] leading-relaxed max-w-[55ch]">
        Available for live walkthrough. We run through real fault scenarios and show you exactly what your operations team would receive.
      </p>
      <a class="btn btn-primary" href="https://catdev.io" target="_blank" rel="noopener">Request a Demo</a>
    </div>
  </div>
</section>

<style>
  @media (max-width: 820px) {
    .demo-grid { grid-template-columns: 1fr; }
    .flex-wrap { flex-direction: column; text-align: center; }
  }
</style>
