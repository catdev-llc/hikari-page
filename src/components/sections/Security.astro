---
const items = [
  { title: 'Tenant isolation', desc: 'Strict data separation between tenants at compute, storage, and network layers.' },
  { title: 'Encryption', desc: 'TLS 1.3 in transit. AES-256 at rest. Key management integrates with your existing KMS.' },
  { title: 'Audit trail', desc: 'Every model decision, trace diff, and operator action is timestamped and stored for postmortem and compliance review.' },
  { title: 'Role-based access', desc: 'Granular RBAC with integration points for existing identity providers. SSO and SAML supported.' },
];
---

<section class="py-[5.5rem] border-b border-hikari-border-soft" id="security">
  <div class="wrap">
    <span class="label rv">Security & Governance</span>
    <h2 class="text-hikari-text-strong font-semibold tracking-tight leading-[1.2] text-[clamp(1.5rem,2.8vw,2.1rem)] mb-3.5 rv">
      Built for environments where compliance is not optional.
    </h2>

    <div class="sec-grid grid grid-cols-4 gap-2.5 mt-5">
      {items.map((item) => (
        <article class="border border-hikari-border rounded-lg bg-hikari-bg-2 p-3.5 rv">
          <h3 class="text-hikari-text-strong font-semibold text-[0.9rem] leading-[1.2] mb-1.5">{item.title}</h3>
          <p class="text-hikari-muted text-[0.78rem] leading-relaxed">{item.desc}</p>
        </article>
      ))}
    </div>
  </div>
</section>

<style>
  @media (max-width: 1020px) {
    .sec-grid { grid-template-columns: repeat(2, 1fr); }
  }
  @media (max-width: 820px) {
    .sec-grid { grid-template-columns: 1fr; }
  }
</style>
